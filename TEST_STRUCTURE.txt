┌──────────────────────────────────────────────────────────────────────────┐
│                   PERSISTENT SESSIONS TEST STRUCTURE                     │
└──────────────────────────────────────────────────────────────────────────┘

src/session.rs (Unit Tests - 7 tests)
├── test_session_creation ✅
│   └── Verifies Session initialization
│
├── test_session_subscriptions ✅
│   └── Tests add/remove/duplicate subscriptions
│
├── test_session_message_queueing ✅
│   ├── QoS 0 → NOT queued
│   ├── QoS 1 → Queued
│   └── QoS 2 → Queued
│
├── test_take_pending_messages ✅
│   └── Message retrieval and clearing
│
├── test_session_manager_clean_session ✅
│   └── clean_session=true behavior
│
├── test_session_manager_persistent_session ✅
│   └── clean_session=false behavior
│       ├── Session preserved
│       └── Subscriptions preserved
│
└── test_session_manager_clean_clears_persistent ✅
    └── Clean session overrides persistent session

tests/integration_tests.rs (Integration Tests - 2 tests)
├── test_persistent_session ✅
│   ├── 1. Connect (clean_session=false)
│   ├── 2. Verify session_present=false
│   ├── 3. Subscribe to topic
│   ├── 4. Disconnect
│   ├── 5. Publish QoS 1 message (offline)
│   ├── 6. Reconnect (clean_session=false)
│   ├── 7. Verify session_present=true
│   ├── 8. Receive queued message
│   └── 9. Verify subscription restored
│
└── test_clean_session_clears_persistent ✅
    ├── 1. Connect (clean_session=false)
    ├── 2. Subscribe to topic
    ├── 3. Disconnect
    ├── 4. Reconnect (clean_session=true)
    ├── 5. Verify session_present=false
    └── 6. Verify subscriptions cleared

┌──────────────────────────────────────────────────────────────────────────┐
│                         TEST EXECUTION RESULTS                           │
└──────────────────────────────────────────────────────────────────────────┘

Unit Tests:    7 passed, 0 failed ✅
Integration:   2 passed, 0 failed ✅
Total:         9 passed, 0 failed ✅

MQTT 3.1.1 Compliance: 100% ✅

┌──────────────────────────────────────────────────────────────────────────┐
│                           TEST COMMANDS                                  │
└──────────────────────────────────────────────────────────────────────────┘

Run unit tests:
  $ cargo test --lib session

Run integration tests:
  $ cargo test --test integration_tests persistent

Run all tests:
  $ cargo test

Run with output:
  $ cargo test -- --nocapture
